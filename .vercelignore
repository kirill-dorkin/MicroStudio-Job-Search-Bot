# Exclude only what we don't want to upload
# Draft/other workspaces
beta-bot/
.venv/
venv/

# Git and OS junk
.git/
.DS_Store
*.log

# Next.js build cache and deps (Vercel installs deps itself)
web/.next/
web/node_modules/
